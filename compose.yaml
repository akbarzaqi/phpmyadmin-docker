services:
  db:
    image: mariadb:10.11
    environment:
      MYSQL_ROOT_PASSWORD: 123
    ports:
      - "3306:3306"

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    environment:
      PMA_HOST: db
    ports:
      - "8080:80"
